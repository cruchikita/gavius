version: '3.8'
services:
  grafana:
    image: grafana/grafana
    restart: always
    ports: 
      - "3000:3000"
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: G@viu5P1lot3
